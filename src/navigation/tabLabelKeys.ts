/** Bottom tab route name → i18n key (labels resolved in PremiumTabBar on locale change). */
export const TAB_LABEL_KEYS: Record<string, string> = {
  Home: 'tabHome',
  Payments: 'tabPayments',
  Statistics: 'tabStatistics',
  Profile: 'tabProfile',
};
